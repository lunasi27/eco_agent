# ECO Agent 项目架构设计

本文档定义 ECO Agent（基于 LangGraph 的 ECO 固定流水线原型）的项目目录结构、模块职责、文件归属和开发约定。

设计原则来自《基于LangGraph的ECO固定流水线详细设计方案》：调用层与图代码分离、Step 粒度 MCP API、Mock 先行可切换、State 集中管理。

---

## 一、顶层目录结构

```
eco_agent/
├── docs/                       # 设计文档（已有）
├── src/                        # 核心源码
├── tests/                      # 测试
├── config/                     # 配置文件
├── scripts/                    # 辅助脚本
├── .env.example                # 环境变量模板
├── .gitignore
├── pyproject.toml              # 依赖与构建配置
└── README.md
```

---

## 二、src/ 目录详解

```
src/
├── __init__.py
├── main.py                     # CLI 入口：argparse + YAML config 加载 + 图编译 + event loop
├── state.py                    # ECOState (TypedDict + Annotated reducers)，25 字段
├── graph_builder.py            # 图编排：节点注册 + 边定义 + 条件边 + Send 并行
├── nodes/                      # 节点实现
│   ├── __init__.py
│   ├── step_runner.py          # run_step 统一辅助函数（耗时追踪 + 异常标记）
│   ├── node_init.py            # make_init_node() — 校验 + 状态初始化
│   ├── node_phase1.py          # make_step_node 工厂 — run_eco_route / run_ext
│   ├── node_phase2.py          # run_sta / run_pv / run_signoff + node_phase2_summary
│   ├── node_phase3.py          # run_pt_fix_setup / run_pt_fix_hold / run_pt_fix_leakage + node_phase3_summary（含 iteration_history）
│   ├── node_gates.py           # ★ phase2_gate / phase3_gate — 并行结果汇聚（架构优化）
│   ├── node_error_handler.py   # ★ make_error_handler_node() — 路由下沉到节点内部 Command(goto)（架构优化）
│   └── node_finalize.py        # node_finalize — 收敛对比 + FINISHED + REPORT.json
├── routers/                    # 条件路由函数
│   ├── __init__.py
│   └── phase_routes.py         # route_after_run_ext（Send 并行 + 共享字段白名单）
│                               # route_after_phase2_gate / route_after_phase3_gate（单一条件路由）
│                               # route_after_phase2_summary（互斥分支路由 + 非法→error_handler）
│                               # route_after_phase3_summary（continue→init / stop→finalize）
├── mcp_server/                 # MCP Server（Step 粒度，10 个 Tool）
│   ├── __init__.py
│   ├── protocol.py             # ECOMCPServer Protocol + STEP_NAMES（10 个 step 注册表）
│   ├── mock.py                 # MockECOMCPServer（7 个 scenario）
│   ├── real.py                 # RealECOMCPServer（读 config.yaml → wrapper 执行 EDA 命令）
│   ├── parsers.py              # 内置 STEP_PARSE_PATTERNS（正则提取 violations，不进用户配置）
│   ├── mcp_app.py              # MCP Server app（Tool 注册 + schema）
│   └── stdio_runner.py         # stdio 传输层入口（ECO_BACKEND 切换 Mock/Real）
├── calling_layer/              # 调用层（薄壳，slash 命令 + event loop）
│   ├── __init__.py
│   ├── commands.py             # slash 命令分发：/help /init /status /run_eco /sessions /resume /new /exit
│   ├── init_command.py         # /init：扫描 EDA 目录 → 生成 eco_agent/config.yaml
│   ├── event_loop.py           # stream → get_state → slash 命令子循环 / Command(resume)
│   └── formatters.py           # format_event / format_interrupt / format_final_state
├── conversation/               # 对话路径
│   ├── __init__.py
│   └── agent.py                # make_agent_entry_node() — LLM/规则意图识别 + chat_fallback
└── utils/                      # 通用工具
    ├── __init__.py
    ├── constants.py            # STEP_TO_PHASE / VALID_STEP_TARGETS / VALID_FIX_STRATEGIES
    ├── config_loader.py        # YAML 加载 + deep merge + DEFAULTS 默认值
    ├── checkpoint_config.py    # Checkpoint 工厂（MemorySaver / SqliteSaver）
    ├── session_store.py        # 会话管理：list / classify / delete thread
    └── file_utils.py           # ensure_dir / check_write_permission / validate_design_dir / find_design_files
```

**架构优化说明**（开发过程中迭代引入）：
- `node_gates.py`：phase2_gate / phase3_gate 解决 Send 并行 + 多目标条件边的 race condition
- `node_error_handler.py` 内部 Command(goto)：绕开 checkpoint metadata 残留导致的额外 interrupt
- calling_layer 极度简化：所有中断统一 `Command(resume=user_input)`，Command(goto) 由节点内部返回
- `config_loader.py` / `checkpoint_config.py` / `formatters.py` / `agent.py` / `node_gates.py` 是开发过程中自然演进的模块，设计文档初始骨架未提及

---

## 三、各模块职责与关键代码归属

### 3.1 src/main.py —— CLI 入口

| 职责 | 说明 |
|---|---|
| YAML 配置加载 | `argparse --config` → `config_loader.load_config()` → deep merge 默认值 |
| Checkpointer 初始化 | 根据 `checkpoint.backend` 创建 MemorySaver 或 SqliteSaver |
| 图编译 | `build_graph(mcp_server, checkpointer, pipeline=cfg["pipeline"])` |
| MCP 后端选择 | `ECO_BACKEND` 环境变量：`mock`（默认）→ MockECOMCPServer，`real` → RealECOMCPServer |
| 交互模式 | `event_loop.run_event_loop(graph, config)` —— 支持 slash 命令 + 对话 + pipeline 中断恢复 |
| 非交互模式 | `--no-interactive` 用于 CI/脚本，自动 stream 到结束 |
| 不做业务逻辑 | main.py 只有 argparse + 初始化 + 启动 |

### 3.2 src/state.py —— ECOState 定义

所有 23 个 State 字段集中在此，使用 `TypedDict` + `Annotated` reducers：

```python
from typing import TypedDict, Annotated
from operator import add

def _last_writer_reducer(existing, updates):
    return updates

def _dict_merge_reducer(existing, updates):
    return {**existing, **updates}

class ECOState(TypedDict, total=False):
    # === 基础参数 ===
    design_name: str
    design_dir: str                    # ★ 架构扩展
    run_dir: str
    iteration_cnt: int
    
    # === Phase1/2 Step 结果 ===
    setup_vio: int
    hold_vio: int
    pv_pass: bool
    signoff_pass: bool

    # === 中断相关 ===
    interrupt_msg: str
    user_fix_strategy: str
    user_iter_choice: str
    user_error_choice: str             # ★ error_handler 架构扩展
    retry_step: str                    # ★ error_handler 架构扩展

    # === 状态跟踪（dict 字段用 _dict_merge_reducer，Phase2 并行时三个节点各自更新不同 key） ===
    phase_status: Annotated[dict[str, str], _dict_merge_reducer]
    step_status: Annotated[dict[str, str], _dict_merge_reducer]
    step_elapsed: Annotated[dict[str, float], _dict_merge_reducer]  # ★ step_runner 耗时追踪
    current_phase: str
    current_step: str
    error_msg: str

    # === 收敛对比 ===
    prev_setup_vio: int
    prev_hold_vio: int
    iteration_history: Annotated[list[dict], _dict_merge_reducer]    # ★ phase3_summary 迭代历史

    # === 对话历史（复用 MessagesState） ===
    messages: Annotated[list, add_messages]
```

**Reducer 策略说明**：
- `_last_writer_reducer`：覆盖式合并，适用于单值字段（setup_vio / hold_vio / run_dir 等）
- `_dict_merge_reducer`：字典键级合并，适用于 phase_status / step_status / step_elapsed（Phase2 并行三个节点同时更新 step_status 的不同 key：run_sta→step_status["run_sta"]="done"、run_pv→step_status["run_pv"]="done"、run_signoff→step_status["run_signoff"]="done"，需要合并而不是覆盖）
- `add_messages`：LangGraph 标准 messages reducer，list 追加

### 3.3 src/graph_builder.py —— 图编排

唯一职责：**把所有节点和边组装成图并返回 compiled graph**。不实现节点逻辑，不写路由函数。

```python
def build_graph(
    mcp_server, checkpointer=None,
    llm_callable=None, skip_agent_entry=False,
    pipeline=None,  # ★ 参数化构图：接收 pipeline dict 决定注册什么
) -> CompiledStateGraph:
    pipeline = pipeline or _DEFAULT_PIPELINE
    phases_cfg = pipeline.get("phases", {})

    p2_steps = tuple(phases_cfg["phase2"]["steps"])
    phase3_cfg = phases_cfg.get("phase3", {})
    p3_steps = tuple(phase3_cfg.get("steps", []))
    p3_router = phase3_cfg.get("router", "user_choice")
    has_phase3 = len(p3_steps) > 0

    builder = StateGraph(ECOState)

    # 1. 固定节点
    builder.add_node("agent_entry", make_agent_entry_node(llm_callable))
    builder.add_node("init", make_init_node(mcp_server))
    builder.add_node("error_handler", make_error_handler_node())    # ★ 内部 Command(goto)
    builder.add_node("finalize", make_finalize_node())

    # 2. Phase1（固定串行）
    builder.add_node("run_eco_route", make_step_node(mcp_server, "run_eco_route", "phase1"))
    builder.add_node("run_ext", make_step_node(mcp_server, "run_ext", "phase1"))
    builder.add_edge("run_eco_route", "run_ext")

    # 3. Phase2（★ 动态并行，闭包捕获 p2_steps）
    for step in p2_steps:
        builder.add_node(step, make_step_node(mcp_server, step, "phase2"))
        builder.add_edge(step, "phase2_gate")  # 每个注册的 step → gate
    builder.add_node("phase2_gate", make_phase2_gate_node(p2_steps))  # ★ 工厂函数
    builder.add_node("phase2_summary", make_phase2_summary_node(       # ★ 工厂函数
        p2_steps, has_phase3=has_phase3, p3_router=p3_router))
    builder.add_conditional_edges("run_ext", make_route_after_run_ext(p2_steps))  # ★ 工厂函数
    builder.add_conditional_edges("phase2_gate", route_after_phase2_gate)

    # 4. Phase3（★ 动态分支，可能跳过）
    if has_phase3:
        for step in p3_steps:
            builder.add_node(step, make_step_node(mcp_server, step, "phase3"))
            builder.add_edge(step, "phase3_gate")
        builder.add_node("phase3_gate", make_phase3_gate_node(p3_steps))
        builder.add_node("phase3_summary", make_phase3_summary_node())
        if p3_router == "user_choice":
            builder.add_conditional_edges("phase2_summary", make_route_after_phase2_summary(p2_steps, p3_steps))
        elif p3_router.startswith("auto_"):
            target = f"run_pt_fix_{p3_router.replace('auto_', '')}"
            builder.add_edge("phase2_summary", target)  # 硬路由，不中断
        builder.add_conditional_edges("phase3_gate", route_after_phase3_gate)
        builder.add_conditional_edges("phase3_summary", route_after_phase3_summary)
    else:
        builder.add_edge("phase2_summary", "finalize")  # ★ 跳过 Phase3

    # 5. 入口和终节点
    builder.add_conditional_edges("agent_entry", route_after_agent_entry)
    builder.add_conditional_edges("init", route_after_init)
    builder.add_edge("finalize", END)
    # ★ error_handler 不加条件边！节点内部返回 Command(goto=target, update=reset)

    return builder.compile(checkpointer=checkpointer)
```

**关键设计**：
- **参数化构图**：`pipeline dict` 决定 Phase2/Phase3 注册哪些 step、Phase3 路由策略。构图时只注册 pipeline 指定的节点和边，运行时图拓扑固定——不会有"注册了但不走"的假节点。
- **工厂函数 + 闭包**：`make_phase2_gate_node(expected_steps)` / `make_route_after_run_ext(p2_steps)` / `make_phase2_summary_node(p2_steps, has_phase3, p3_router)` 闭包捕获固定参数，运行时不需要读 State，State 里没有开关字段。
- **config_loader 防呆**：构图前用 `STEP_TO_PHASE` 注册表校验 pipeline 配置，跨 phase 错配 / 非法 router / 空 phase2 都会在 1 秒内报错退出。
- 默认 pipeline（`_DEFAULT_PIPELINE`）等价于旧版硬编码完整流程，向后兼容。

### 3.4 src/nodes/ —— 节点实现

| 文件 | 包含的节点 | 对应设计文档 |
|---|---|---|
| `step_runner.py` | `_run_step(state, step_name, phase_name, result_keys, extra_params)` | 4.5 节统一辅助函数 |
| `node_init.py` | `make_init_node()` → 返回闭包 `node_init(state)` | 4.1 节 |
| `node_phase1.py` | `make_step_node(mcp_server, "run_eco_route", ...)` 和 `make_step_node(mcp_server, "run_ext", ...)` 的工厂函数 + `node_phase1_summary`（如果有的话） | 4.2 节 |
| `node_phase2.py` | run_sta / run_pv / run_signoff 三个 Step 工厂 + `node_phase2_summary` | 4.3 节 |
| `node_phase3.py` | run_pt_fix_setup / run_pt_fix_hold 工厂 + `node_phase3_summary` | 4.4 节 |
| `node_error_handler.py` | `make_error_handler_node()` → 返回闭包 `node_error_handler(state)` | 4.6 节 |
| `node_finalize.py` | `node_finalize(state)` | 4.7 节 |

**命名约定**：工厂函数用 `make_xxx_node()`，返回的闭包内部修改 State 并 return，闭包名用 `node_xxx`。Phase 的 Step 节点直接通过 `step_runner.py` 的工厂创建，不单独写闭包。

### 3.5 src/routers/phase_routes.py —— 路由函数

所有条件边的 path 参数集中在这里：

```python
# agent_entry
def route_after_agent_entry(state) -> Literal["init", "__end__"]: ...

# Init
def route_after_init(state) -> Literal["run_eco_route", "error_handler"]: ...

# Phase1 串行
def route_after_run_ext(state) -> list[Send]: ...
# ★ 返回 Send 并行，arg 用共享字段白名单：
# _PHASE2_SHARED_KEYS = ("design_name", "design_dir", "run_dir",
#                        "iteration_cnt", "prev_setup_vio", "prev_hold_vio",
#                        "iteration_history", "step_elapsed")

# Phase2 Gate 汇聚后 — 单一条件路由
def route_after_phase2_gate(state) -> Literal["error_handler", "phase2_summary"]: ...
# ★ 任意并行 Step error → error_handler（方案 A：全阻断，无警告级）

# Phase2 Summary → Phase3 互斥分支
def route_after_phase2_summary(state) -> Literal["run_pt_fix_setup", "run_pt_fix_hold", "run_pt_fix_leakage", "error_handler"]: ...

# Phase3 Gate 汇聚后 — 单一条件路由
def route_after_phase3_gate(state) -> Literal["error_handler", "phase3_summary"]: ...

# Phase3 Summary → 迭代循环 或 结束
def route_after_phase3_summary(state) -> Literal["init", "finalize"]: ...
```

**约束**：路由函数是纯函数——只读 State，不修改 State，不调 MCP API。

**Send 共享字段白名单**（`route_after_run_ext` 使用）：Phase2 Send 并行时只传 `_PHASE2_SHARED_KEYS` 子集给每个并行节点，避免并行节点互相覆盖各自独立更新的字段（如 setup_vio、hold_vio、pv_pass）。

### 3.6 src/mcp_server/ —— MCP Server（Mock + Real 双后端）

| 文件 | 职责 |
|---|---|
| `protocol.py` | `ECOMCPServer` Protocol + `STEP_NAMES`（10 个 step 注册表，单一来源） |
| `mock.py` | `MockECOMCPServer` —— 7 个 scenario 驱动的 Mock 实现 |
| `real.py` | `RealECOMCPServer` —— 读 `config/config.yaml` → resolve 占位符 → 写临时命令文件 → subprocess 调 `run_step_wrapper.csh` → `parse_output` 提取 violations |
| `parsers.py` | `STEP_PARSE_PATTERNS` + `parse_output(step_name, text)` + `coerce_value()` —— 内置正则，不进用户配置 |
| `mcp_app.py` | MCP Server app（Tool 注册 + input schema） |
| `stdio_runner.py` | stdio 传输层入口，`ECO_BACKEND` env 切换 Mock/Real |

**关键约束**：Mock 和 Real 都必须实现 Protocol 的 10 个方法，签名完全一致。`ECO_BACKEND` 环境变量决定实例化哪个。Real 模式的配置详见 [docs/MCP Server 详细设计.md](MCP%20Server%20详细设计.md)。

### 3.7 src/calling_layer/ —— 调用层（薄壳 + slash 命令）

| 文件 | 职责 |
|---|---|
| `event_loop.py` | 主循环：`stream → get_state → check interrupt` → slash 命令子循环 / `Command(resume=user_input)` |
| `commands.py` | slash 命令分发：`/help` `/init` `/status` `/run_eco` `/sessions` `/resume` `/new` `/exit` |
| `init_command.py` | `/init`：扫描 EDA 工作目录（`N.APR.{design}` 等模式）→ 生成 `eco_agent/config.yaml` |
| `formatters.py` | `format_event()` / `format_interrupt()` / `format_final_state()` 三个纯格式化函数 |

**event_loop 工作方式**：图 stream 完成后检查是否挂在 interrupt。若是，进入 slash 命令子循环——`/` 开头的输入走 `dispatch()` 本地处理（图保持挂起），其他输入作为 `Command(resume=user_input)` 传给图。`/run_eco` 合成自然语言 resume 输入；`/resume` `/new` 切换会话（改写 thread_id 后重新 stream）；`/exit` 在流水线中断时保留断点退出。

**关键约束**：调用层**不 import LangGraph 内部的任何节点/路由函数/State 结构实现**——它只和 `graph`（编译后的 CompiledStateGraph）打交道，调 `stream()` / `get_state()`。这样未来重构为 Typer CLI 时整个 calling_layer 可以直接替换，graph 代码一行不动。

### 3.8 src/conversation/ —— 对话路径

| 文件 | 职责 |
|---|---|
| `agent.py` | `make_agent_entry_node()` — LLM/规则双重意图识别 + design_name 提取 + chat_fallback 路由（意图非 ECO 时终止对话） |

**实现方式**：支持规则匹配（直接从 messages 提取 design_name）和 LLM 两种路径，优先规则匹配（快、确定），规则匹配失败时走 LLM。没有使用 LangChain ToolNode——原型阶段意图分流简单，规则 + LLM 足够。

### 3.9 src/utils/ —— 通用工具

| 文件 | 职责 |
|---|---|
| `constants.py` | `STEP_TO_PHASE` / `VALID_STEP_TARGETS` / `VALID_FIX_STRATEGIES` / `INITIAL_PHASE_STATUS` 等常量 |
| `config_loader.py` | YAML 加载 + deep merge（默认值 ← 用户配置）+ `DEFAULTS` dict 定义默认配置 |
| `checkpoint_config.py` | Checkpoint 工厂：根据 `checkpoint.backend` 配置返回 MemorySaver 或 SqliteSaver |
| `session_store.py` | 会话管理：`describe_sessions()` 列表 / `classify_snapshot()` 分类 / `delete_thread()` 删除 |
| `file_utils.py` | `ensure_dir(path)` / `check_write_permission(path)` / `validate_design_dir(path)` / `find_design_files(path)` |

---

## 四、tests/ 目录详解（对应 7 层测试）

```
tests/
├── __init__.py
├── conftest.py                 # 全局 fixture：MockECOMCPServer 实例、checkpointer 实例、thread_id
├── l1_mcp_api/                 # L1：MCP API 单元测试
│   ├── __init__.py
│   ├── test_mock_protocol.py   # Mock 是否严格遵守 Protocol（签名一致性）
│   ├── test_mock_return_schema.py  # Mock 返回值字段是否和设计文档一致
│   ├── test_mock_exception_passthrough.py  # Mock 异常是否能正确抛
│   └── scenarios/              # 各 scenario 的预设数据单独验证
│       └── test_scenario_data.py
├── l2_nodes/                   # L2：LangGraph 节点单元测试
│   ├── __init__.py
│   ├── test_run_step.py        # _run_step 正常/异常/result_keys 安全
│   ├── test_route_functions.py  # 10 个路由函数的每个分支
│   ├── test_node_init.py
│   ├── test_node_phase2_summary.py  # 并行聚合后 State 是否正确
│   ├── test_node_phase3_summary.py
│   ├── test_node_error_handler.py  # current_phase 是否设为 error_handler
│   └── test_node_finalize.py
├── l3_graph_topology/          # L3：图拓扑集成测试
│   ├── __init__.py
│   ├── test_phase1_serial.py          # run_eco_route → run_ext 串行流转
│   ├── test_phase2_parallel.py        # Send API 并行 + State 聚合
│   ├── test_phase2_summary_branch.py  # phase2_summary → Phase3 三条分支
│   ├── test_phase3_loop.py            # continue → init / stop → finalize
│   ├── test_error_handler_routing.py  # 各 Step 异常都能路由到 error_handler
│   └── test_full_topology.py          # 全拓扑注册验证
├── l4_interrupt_recovery/      # L4：中断恢复专项
│   ├── __init__.py
│   ├── test_interrupt1_trigger.py        # phase2_summary 正常中断
│   ├── test_interrupt1_position.py       # interrupt() 前 prev_* 已写入
│   ├── test_interrupt1_resume.py         # Command resume → phase3
│   ├── test_interrupt2_trigger.py
│   ├── test_interrupt2_resume_continue.py  # Command resume continue → init
│   ├── test_interrupt2_resume_stop.py      # Command resume stop → finalize
│   ├── test_error_handler_resume_retry.py  # Command goto → 出错 Phase 首 Step
│   ├── test_error_handler_resume_abort.py  # Command goto → finalize
│   └── test_interrupt_rerun_safety.py     # interrupt rerun 不产生副作用
├── l5_error_handling/          # L5：错误处理专项
│   ├── __init__.py
│   ├── test_phase1_run_ext_error.py      # Mock scenario=phase1_ext_error
│   ├── test_phase2_sta_error.py          # Mock scenario=phase2_sta_error
│   ├── test_phase2_pv_error.py
│   ├── test_phase2_signoff_error.py
│   ├── test_phase3_fix_error.py
│   └── test_all_errors_route_to_handler.py
├── l6_idempotency/             # L6：幂等性专项
│   ├── __init__.py
│   ├── test_interrupt_rerun_state_consistency.py  # rerun 节点不重复写 prev_*
│   ├── test_retry_no_side_effect.py               # retry 重跑 Phase 后 State 一致
│   └── test_state_update_immutability.py          # 所有 State 更新用 {**old, new}
├── l7_e2e/                     # L7：E2E 测试
│   ├── __init__.py
│   ├── test_happy_path_complete.py    # 完整 happy path（单轮迭代）
│   ├── test_multi_iteration.py        # 多轮迭代（Mock convergence scenario）
│   ├── test_process_restart.py        # SqliteSaver + kill 进程 + 恢复
│   └── test_error_retry_flow.py       # 错误 → Error Handler → retry → 继续
└── fixtures/                    # 测试数据
    ├── __init__.py
    ├── mock_designs.py          # 预设 design_name / run_dir / iteration_cnt
    └── sample_logs.py           # 模拟日志内容用于 Finalize 测试
```

---

## 五、config/ 目录详解

```
config/
├── default.yaml                 # LangGraph 框架级配置（模型选择、checkpoint、pipeline、init 校验）
├── production.yaml              # 生产环境覆盖（严格校验）
└── config.yaml                  # MCP Server Real 模式配置（Part 1 项目级 + Part 2 运行级）
```

**两层配置**：

- `default.yaml` / `production.yaml`：LangGraph 框架级配置，随代码仓库走，通过 `--config` 加载。
- `config.yaml`：MCP Server Real 模式配置（step_command / base / timeout_s / design_name 等），通过 `ECO_CONFIG` 环境变量或 `RealECOMCPServer(config_path=...)` 加载。可用 `/init` 命令扫描 EDA 工作目录自动生成。

`default.yaml` 示例：

```yaml
eco_agent:
  default_scenario: "happy_path"
  default_design_name: "demo_design"

checkpoint:
  backend: "sqlite"              # "memory" | "sqlite"
  db_path: "runs/checkpoints.sqlite"
  check_same_thread: false

llm:                              # LLM 配置（enabled=true 时密钥缺失会报错退出）
  enabled: true
  provider: "openai"              # 客户端协议（DeepSeek/豆包等均走 openai 兼容）
  model: "deepseek-chat"
  base_url: "https://api.deepseek.com/v1"
  api_key_env: "DEEPSEEK_API_KEY" # 密钥所在环境变量名（与 provider 解耦）
  temperature: 0

mock_server:
  simulate_delay: 0.3
  write_logs: true

init_validation:
  require_design_dir: false      # 原型阶段关闭严格校验
  require_netlist: false
  require_sdc: false
  required_env_vars: []
  required_tools: []

finalize:
  write_json_report: true
  write_finished_marker: true
```

**加载方式**：`main.py` 通过 `argparse --config` 指定 YAML 路径，`config_loader.load_config()` 做 `deep_merge(DEFAULTS, yaml.safe_load(f))`。不指定时使用内置 `DEFAULTS`。

---

## 六、开发顺序与模块依赖关系

```
                    ┌─────────────┐
                    │  pyproject   │
                    │  + .env      │
                    └──────┬──────┘
                           │
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
   ┌─────────┐       ┌──────────┐       ┌───────────────┐
   │  state  │       │ constants │       │ mcp_server/   │
   └────┬────┘       └────┬─────┘       └──────┬────────┘
        │                 │                     │
        ▼                 ▼                     │
   ┌────────────────────────┐                  │
   │      nodes/            │◄─────────────────┘
   │  (step_runner 依赖     │    nodes/step_runner 调 MCP API
   │   mcp_server 实例)     │
   └────────┬───────────────┘
            │
            ▼
   ┌────────────────────────┐
   │      routers/          │    路由函数只读 state，不依赖 nodes
   └────────┬───────────────┘
            │
            ▼
   ┌────────────────────────┐
   │    graph_builder.py    │    组装 nodes + routers → compiled graph
   └────────┬───────────────┘
            │
            ▼
   ┌────────────────────────┐
   │   calling_layer/       │    只依赖 compiled graph，不依赖 nodes/routers
   └────────┬───────────────┘
            │
            ▼
   ┌────────────────────────┐
   │      main.py           │    入口：初始化 → graph_builder → calling_layer
   └────────────────────────┘
```

**依赖方向**（严格单向，禁止反向）：

| 依赖方 | 被依赖方 |
|---|---|
| `nodes/step_runner.py` | `state`、`mcp_server`（传入的实例）、`utils` |
| `routers/phase_routes.py` | `state`、`utils/constants`（只读） |
| `graph_builder.py` | `state`、`nodes/`、`routers/`、`utils` |
| `calling_layer/ | **只依赖** `langgraph` 类型 + `utils/constants`（VALID_FIX_STRATEGIES），**不依赖** nodes/routers/graph_builder），**不依赖** nodes/routers/graph_builder |
| `main.py` | `graph_builder`、`calling_layer`、`mcp_server`（Mock/Real 二选一）、`config` |

**为什么调用层不依赖 graph_builder**：调用层只和 `CompiledStateGraph`（编译后的可执行图）打交道。如果调用层 import 了 nodes/routers，那重构为 Typer CLI 时就会牵一发而动全身——违反设计文档 6.1 节"调用层可独立替换"的约束。

---

## 七、测试与源码的镜像关系

| 测试层 | 主要测试对象 | 源码目录 |
|---|---|---|
| L1 MCP API 单元 | Mock/Real 实现 | `src/mcp_server/` |
| L2 节点单元 | `_run_step` + 路由函数 + 汇总节点 | `src/nodes/` + `src/routers/` |
| L3 图拓扑 | 节点注册 + 边定义正确性 | `src/graph_builder.py` |
| L4 中断恢复 | interrupt 位置 + Command resume/goto | `src/nodes/` + `src/calling_layer/` |
| L5 错误处理 | Error Handler + retry | `src/nodes/node_error_handler.py` + `src/calling_layer/` |
| L6 幂等性 | State 更新方式 + retry 副作用 | `src/nodes/step_runner.py` + `src/state.py` |
| L7 E2E | 完整流程 + Checkpoint | 全部 |

---

## 八、pyproject.toml 核心依赖建议

```toml
[project]
name = "eco-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "langgraph>=0.2.0",
    "langchain>=0.3.0",
    "langchain-openai>=0.2.0",
    "pyyaml>=6.0",             # ★ 加载 config/*.yaml
    "mcp>=1.0.0",              # MCP Python SDK
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=5.0.0",
    "ruff>=0.4.0",
]
```

**说明**：
- 不使用 `langgraph-checkpoint-sqlite` 独立包，直接用 `langgraph.checkpoint.sqlite.SqliteSaver`（LangGraph ≥ 0.2.0 内置）
- `pyyaml` 是配置加载核心依赖（替代原设计的 `tomli` 读 TOML）
- `checkpoint_config.py` 自己实现 SqliteSaver 初始化（自动创建目录 + 设置 check_same_thread=False）

---

## 九、开发里程碑与目录产出

| 里程碑 | 对应设计文档 Step | 需要产出的目录/文件 | 完成标志 |
|---|---|---|---|
| M1：对话 Agent + State | Step 1 | `src/state.py` + `src/conversation/` + `src/calling_layer/` 骨架 + `tests/l1_l2_对话部分` | 跑通多轮对话 + 意图分流 |
| M2：Mock MCP + Step 节点 | Step 2 | `src/mcp_server/protocol.py` + `mock.py` + `src/nodes/step_runner.py` + Phase1 Step 节点 + `tests/l1_mcp_api` + `tests/l2_nodes` | Mock scenario 跑通单个 Step |
| M3：图编排 + 并行 + 中断 + Error Handler | Step 3 | `src/graph_builder.py` + `src/nodes/` 全部节点 + `src/routers/phase_routes.py` + `tests/l3_graph_topology` + `tests/l4_interrupt` + `tests/l5_error_handling` | L1~L5 全绿，happy_path 完整跑通 |
| M4：幂等性 + Checkpoint | Step 3.5 | `tests/l6_idempotency` + SqliteSaver 接入 + `calling_layer/retry_strategy.py` 完善 | 进程 kill 后断点续跑成功 |
| M5：真实 EDA 接入 | Step 4 | `src/mcp_server/real.py` + `parsers.py` + `scripts/eda/run_step_wrapper.csh` + `config/config.yaml` | ✅ 已完成：Real 模式 + wrapper + BSD csh 集成测试 + `/init` 自动生成配置 |
| M6：Typer CLI 重构（可选） | Step 5 | 重写 `src/calling_layer/` 为 Typer CLI | 图代码零改动，CLI 替换成功 |

---

## 十、编码约定

| 约定 | 说明 |
|---|---|
| 节点函数签名 | 所有 LangGraph 节点闭包签名统一为 `def node_xxx(state: ECOState) -> dict:`（返回 State 的更新 dict，不 mutate 原始 state） |
| 路由函数签名 | 统一为 `def route_after_xxx(state: ECOState) -> Literal["node_a", "node_b", ...]:` 或 `-> list[Literal["node_a", ...]]`（Send 并行时） |
| State 更新方式 | **禁止** `state["setup_vio"] = 123`；**必须** `{"setup_vio": 123, ...}` 作为返回值，LangGraph 自动合并 |
| 异常处理 | 节点内部只 try-except `_run_step`（调用 MCP 的地方），其他代码不 try-except；异常类型统一为 `Exception` 子类 |
| 日志 | 原型阶段 `print()` + `logging.basicConfig(level=logging.INFO)` 够用；不引入重型日志框架 |
| import 规范 | 每个文件顶部只 import 自己直接依赖的模块；禁止 `import *`；LangGraph 类型注解用 `from __future__ import annotations` |
| 常量 | 所有跨文件共享的常量放 `src/utils/constants.py`，禁止在多个文件里重复定义 |

---

## 十一、架构演进策略（流程可配置性）

### 11.1 核心决策：3~5 个固定流程 → 硬编码 + 公共辅助函数

**问题**：当前 ECO 流程被固化成 LangGraph 图。未来如果需要支持 3~5 个固定业务流程，是应该继续硬编码还是引入 YAML 声明拓扑？

**决策**：维持硬编码（每个流程一个独立的 builder 函数）+ 抽公共辅助函数消除重复。**不引入 YAML 声明拓扑**。

**决策日期**：2026-09-13（原型开发收尾阶段）

---

### 11.2 决策依据

#### 为什么不选 YAML 声明拓扑？

每个业务流程**几乎一定会有独特细节**，这些细节用 YAML 很难优雅表达：

| 细节 | YAML 表达方式 | 问题 |
|---|---|---|
| Phase2 Send 用共享字段白名单（不传完整 state） | `send_shared_keys: [design_name, run_dir, ...]` | 跑通但如果某个流程 Send arg 要做 transform（如 `.upper()`），YAML 开始崩 |
| Phase3 三条普通边全注册（LangGraph 要求所有可达路径） | `steps: [a, b, c]` + `type: branch` | OK，但如果某个流程 Phase3 只注册两条边呢？YAML 没有表达力 |
| error_handler 不加任何边（节点内部 Command(goto)） | `edges: none` 或 `self_routing: true` | 每加一个 LangGraph 高级特性，YAML schema 膨胀一点 |
| 某个 Phase 不需要 interrupt | `skip_interrupt: true` | OK，但如果某个流程 Phase2 summary 不是中断而是条件路由到 finalize 呢？ |

**YAML 的根本问题**：通用 Builder 为了覆盖"几乎一定会有的例外"，会膨胀到变成一个不完整的 Python 解释器。花了 YAML 的维护成本，但没拿到 YAML 的简洁。

#### 为什么硬编码 + 公共辅助函数好？

| 维度 | 优势 |
|---|---|
| LangGraph 特性 100% 覆盖 | Send 共享字段白名单、Gate 自定义判断、三条普通边全注册、Command(goto) 返回类型——想用什么用什么，不受 schema 限制 |
| IDE 完美支持 | `builder.add_edge("run_sta", "phase2_gate")` 是符号，可跳转/重构/类型检查 |
| 调试友好 | 断点下到具体 builder，一眼看清走了哪条边；YAML 需要跨三层跳（YAML → Builder → 生成图） |
| Git diff 清晰 | 边从 A 改到 B，diff 就是一行改动；YAML schema 膨胀时 diff 噪声大 |
| 爆炸半径小 | 改 hold_only builder → 只有 hold_only 崩；改 YAML 通用 Builder → 所有流程全崩 |
| DRY 通过公共辅助函数解决 | `_wire_core_components()` / `_wire_parallel_phase()` / `_wire_branch_phase()` 消除重复，每个流程 builder 只剩"声明差异" |

**硬编码反而是最诚实的选择**——每个流程的拓扑就是 Python 代码，想加什么 LangGraph 特性加什么。

---

### 11.3 演进分层

#### Layer 0：✅ 已实现 — 参数化构图（1 个 builder，pipeline dict 配置）

```python
# src/graph_builder.py —— 参数化构图，pipeline dict 决定拓扑
def build_graph(mcp_server, checkpointer=None, pipeline=None):
    pipeline = pipeline or _DEFAULT_PIPELINE  # 默认完整流程
    p2_steps = tuple(pipeline["phases"]["phase2"]["steps"])
    p3_steps = tuple(pipeline["phases"].get("phase3", {}).get("steps", []))
    # 根据 pipeline 动态注册节点/边/条件边
    ...
    return builder.compile(checkpointer=checkpointer)

# 使用示例
default_graph   = build_graph(mcp, saver)                              # 默认完整流程
sta_only_graph  = build_graph(mcp, saver, pipeline={"phases": {...}})  # STA-only + 跳过 Phase3
hold_graph      = build_graph(mcp, saver, pipeline={..., "router": "auto_hold"})
```

**当前已实现能力**：Phase2 step 可裁剪、Phase3 可跳过、Phase3 router 策略可配置、config_loader 构图前校验。Gate/Route/Summary 全工厂函数化闭包捕获参数，运行时图拓扑固定。CLI 支持 `--pipeline-steps-phase2` / `--skip-phase3` / `--p3-router` 直接覆盖。

#### Layer 1：3~5 个固定流程（硬编码 + 公共辅助函数）

> **Layer 1 = Layer 0 的自然延伸**：当需要多个有本质差异的流程（不只是 step 裁剪，而是拓扑结构不同）时，从 `build_graph(pipeline)` 再抽一层公共辅助函数，每个流程一个独立 builder 函数。

```python
# src/graph_builders.py —— 多流程，公共辅助函数消除重复

def _wire_core_components(builder, mcp_server):
    """注册 agent_entry / init / error_handler / finalize + Phase1 串行"""

def _wire_parallel_phase(builder, step_names, gate_name, phase_name):
    """N 个并行 Step → Gate 汇聚 → 条件路由"""

def _wire_branch_phase(builder, step_names, gate_name, phase_name, router):
    """M 个互斥分支 → Gate 汇聚 → 条件路由"""

# ============ 每个流程 builder 只写"声明差异" ============

def build_default_eco(mcp_server, checkpointer):
    builder = StateGraph(ECOState)
    _wire_core_components(builder, mcp_server)
    _wire_parallel_phase(builder, ["run_sta", "run_pv", "run_signoff"], "phase2_gate", "phase2")
    builder.add_node("phase2_summary", make_phase2_summary_node())
    builder.add_conditional_edges("phase2_summary", route_after_phase2_summary)
    _wire_branch_phase(builder, ["run_pt_fix_setup", "run_pt_fix_hold", "run_pt_fix_leakage"],
                       "phase3_gate", "phase3", route_after_phase2_summary)
    builder.add_node("phase3_summary", make_phase3_summary_node())
    builder.add_conditional_edges("phase3_summary", route_after_phase3_summary)
    builder.set_entry_point("agent_entry")
    builder.add_edge("finalize", END)
    return builder.compile(checkpointer=checkpointer)

def build_hold_only(mcp_server, checkpointer):
    """简化版：Phase2 只跑 STA + PV，Phase3 只跑 fix_hold，硬路由不中断"""
    builder = StateGraph(ECOState)
    _wire_core_components(builder, mcp_server)
    _wire_parallel_phase(builder, ["run_sta", "run_pv"], "phase2_gate", "phase2")
    builder.add_node("phase2_summary", make_phase2_summary_node(skip_interrupt=True))
    _wire_branch_phase(builder, ["run_pt_fix_hold"], "phase3_gate", "phase3",
                       lambda s: "run_pt_fix_hold")
    builder.add_node("phase3_summary", make_phase3_summary_node(force_stop=True))
    builder.set_entry_point("agent_entry")
    builder.add_edge("finalize", END)
    return builder.compile(checkpointer=checkpointer)

def build_regression_only(mcp_server, checkpointer):
    """验证版：Phase2 全部跑，跳过 Phase3，Phase2_summary → finalize"""
    builder = StateGraph(ECOState)
    _wire_core_components(builder, mcp_server)
    _wire_parallel_phase(builder, ["run_sta", "run_pv", "run_signoff"], "phase2_gate", "phase2")
    builder.add_node("phase2_summary", make_phase2_summary_node(skip_interrupt=True))
    builder.add_edge("phase2_summary", "finalize")  # 普通边，跳过 Phase3
    builder.set_entry_point("agent_entry")
    builder.add_edge("finalize", END)
    return builder.compile(checkpointer=checkpointer)
```

**新增流程成本**：复制一个 builder → 改差异部分 → ~10 分钟，不影响现有流程。

#### Layer 2：20+ 个流程（YAML + 硬编码逃生舱）

当流程数量膨胀到 20+，且其中大部分是标准变体（没有独特细节）时，考虑引入 YAML 声明。

**但必须保留硬编码作为逃生舱**——YAML 搞不定的复杂流程（有独特 Send 白名单、特殊 Gate 逻辑、自定义 Command(goto)）继续硬编码。公共辅助函数（`_wire_core_components` / `_wire_parallel_phase` / `_wire_branch_phase`）本身就是 YAML Builder 的基础，演进路径连续。

```python
# 注册机制：YAML 和硬编码并存
PIPELINE_REGISTRY = {
    "default":    build_default_eco,           # 硬编码
    "hold_only":  build_hold_only,             # 硬编码
    "hold_heavy": build_from_yaml("hold_heavy.yaml"),  # YAML
    "lvs_light":  build_from_yaml("lvs_light.yaml"),   # YAML
    "custom_xxx": build_custom_xxx,            # 硬编码逃生舱
}
```

#### Layer 3：流程本身不确定（LLM 驱动动态编排）

当业务流程本身不确定（"我想做 ECO 但不确定先跑 STA 还是先跑 ERC，你帮我决定"），不应该用固定图——换架构到 LangGraph ToolNode Agent：

```python
@tool
def run_sta(design_name: str, run_dir: str) -> dict: ...

@tool
def run_pt_fix_setup(design_name: str, run_dir: str, fix_strategy: str) -> dict: ...

agent = create_tool_calling_agent(llm, [run_sta, run_pv, run_pt_fix_setup, ...])
```

**这不是演进，是换架构**。从"固定图 → LLM Agent"跳跃需要完全不同的测试策略（LLM 行为不可预测）。当前固定图作为"确定性保底"，Agent 作为"灵活性补充"——两者可以并存。

---

### 11.4 演进路径拐点

| 流程数量 | 推荐方案 | 核心指标 |
|---|---|---|
| **1~10 个** | **硬编码 + 公共辅助函数**（Layer 1） | 每个流程 builder ≤ 80 行；公共辅助函数 ≤ 50 行/个 |
| 10~20 个 | 混合：简单流程用 YAML，复杂流程硬编码 | YAML schema 不超过 50 个字段；硬编码作为逃生舱 |
| 20+ 个 | YAML 声明 + 硬编码逃生舱（Layer 2） | 超过 70% 流程能用 YAML 覆盖 |
| 流程本身不确定 | ToolNode Agent（Layer 3） | 业务场景不再是"固定流水线" |

**演进是连续的**：Layer 1 的公共辅助函数就是 Layer 2 YAML Builder 的基础。Layer 2 的注册机制（`PIPELINE_REGISTRY`）不影响 Layer 1 的硬编码流程。

---

### 11.5 未来可配置化预存设计（轻量抽象）

当前 Layer 1 阶段，以下预存设计为未来演进留好口子（~30 行改动，可立即实施）：

```python
# constants.py —— Phase 步骤清单（公共辅助函数的输入）
PHASE_STEPS = {
    "phase1": ("run_eco_route", "run_ext"),
    "phase2": ("run_sta", "run_pv", "run_signoff"),
    "phase3": ("run_pt_fix_setup", "run_pt_fix_hold", "run_pt_fix_leakage"),
}

# nodes/node_gates.py —— gate 工厂化（从硬编码 expected_steps 改为参数化）
def make_gate_node(phase_name: str, expected_steps: tuple[str, ...]):
    def gate(state: ECOState) -> dict:
        return _phase_gate(state, phase_name, expected_steps)
    return gate

# graph_builder.py —— 用 make_gate_node(PHASE_STEPS["phase2"]) 替代硬编码 node_phase2_gate
```

这样 Phase2 加 run_erc 只需改 `PHASE_STEPS["phase2"]` 加一个名字，不需要改 gate 节点代码。这是 Layer 1 内部的 DRY 优化，**不是 Layer 2 的 YAML**。

